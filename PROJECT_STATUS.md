# Bowling project status

This is the single authoritative repository status record.

```ini
PROJECT_NAME=Bowling
PRODUCT_CLASS=STANDALONE_PRODUCT
CANONICAL_REPOSITORY=Drago1068/Bowling
CANONICAL_LOCAL_PATH=C:\Users\Drago\Documents\Bowling
REMOTE_ORIGIN=https://github.com/Drago1068/Bowling.git
DEFAULT_BRANCH=arch/001-domain-sync-foundation
CURRENT_BRANCH=arch/001-domain-sync-foundation
REVIEWED_SOURCE_HEAD=4077c2bf10d883f64559e314f2a5d05fb1769b0f
REVIEWED_SOURCE_TREE=38095255d1d596f5a39f71768d77660c0ad4fa1d
REMEDIATION_HEAD=c04d46930b9644a05505615170892754830a0572
REMEDIATION_TREE=69f005eff08c8991bbc539e4c27c1a8c84b803b9
SLICE_4_NATIVE_ACCEPTANCE=d55743ea0bbcfae28755cd2d19bc6d44eecf39de
CURRENT_HEAD=d82ed963211cd7a80a774b1b816ab10859026e01
CURRENT_TREE=a6cfb602e85dd34164921e8174e2d9fb9d908819
B1_ACCEPTED_COMMIT=d8256794eefdf877f3c847b25f204ec38dc3f9b5
B1_ACCEPTED_TREE=0188acfc45befaed9a05cf18a419fc6951bbc0e4
B1_LOCAL_INTEGRATION_FF_TIP=9305a46707fe5c234b0901eda9e46c14114ea571
B1_LOCAL_INTEGRATION=PASS
B1_INTEGRATED_ON_DEFAULT_BRANCH=true
B1_INTEGRATION_PENDING=false
B1_INTEGRATION_PUSHED_COMMIT=d82ed963211cd7a80a774b1b816ab10859026e01
B1_INTEGRATION_PUSHED_TREE=a6cfb602e85dd34164921e8174e2d9fb9d908819
FEATURE_BRANCH=codex/b1-pin-detail-capture
FEATURE_BRANCH_TIP=9305a46707fe5c234b0901eda9e46c14114ea571
FEATURE_BRANCH_PUSHED=false
SLICE_6_TAG_COMMIT=94cfc4b8ef54cc7501b9dd143d948566fd861fa2
CURRENT_ARCHITECTURE=ARCH-001
CURRENT_SLICE=ARCH-001_SLICE_6_SCORING_FIRST_DEVICE_SHELL
ARCH_001_SLICE_4=FORMALLY_CLOSED
ARCH_001_SLICE_4_IMPLEMENTATION_ACCEPTED=true
ARCH_001_SLICE_4_DEVICE_ACCEPTANCE=PASS
CURRENT_ACCEPTED_CAPABILITY=DETERMINISTIC_OFFLINE_SINGLE_BOWLER_TEN_PIN_SCORING_WITH_HISTORY_RESUME_AND_SCORING_FIRST_SHELL
CURRENT_GATE=B1_INTEGRATION_PUSHED
GATE_STATUS=B1_INTEGRATION_PUSH_CLOSEOUT
B1_IMPLEMENTATION_AUTHORIZED=true
B1_IMPLEMENTATION_EVIDENCE=docs/evidence/arch-001-b1-pin-detail-capture.md
B1_DEVICE_ACCEPTANCE=PASS
B1_FIX_EXIT_ACCESSIBILITY=PASS
B1_APK_SHA256=9141fb073c8a5a0a1c6a2659cc29c0cd57136a1e5685a091ac5e99f42ecbdba4
B1_PRIOR_DEVICE_APK_SHA256=b7f81c910d889b2f8d4abfa7ca18664616be7109473a6d70a9a052fd10328b4c
B1_FORMAL_ACCEPTANCE=true
B1_FINAL_REVIEW_VERDICT=PASS
B1_REAL_LANE_TRIAL=HOLD
B1_REAL_LANE_TRIAL_BLOCKER=USER_TRIAL_OUTCOME_NOT_PROVIDED
B1_SEQUENTIAL_ENTRY_REMEDIATION=PASS_FOCUSED_TESTS
B1_UPDATED_APK_DEVICE_VERIFICATION=PASS
USER_PIN_SELECTOR_ACCEPTANCE=ACCEPTED_DEVICE_VERIFIED
USER_PIN_SELECTOR_HOLD_REASON=
PROPOSED_CAPTURE_DEFAULT=RACK_FIRST_SELECTED_MEANS_STANDING
PROPOSED_CAPTURE_DEFAULT_STATUS=ACCEPTED_IMPLEMENTED
USER_DESIGN_ACCEPTANCE=ACCEPTED
GRAPHICAL_ENTRY_REFINEMENTS=TENTH_X_NOTATION,CONTEXT_AWARE_SPARE,AUTO_ADVANCE_AFTER_SAVE,HOME_METRICS,DATE_HEADER_AVERAGE,GUTTER_BALL2_SPARE,FIX_DEFAULT_FIRST_THROW
GRAPHICAL_ENTRY_PROTOTYPE=prototypes/graphical-entry/index.html
FIELD_VALIDATION=PAUSED
COMMITS_CREATED=true
PUSH_PERFORMED=true
TAG_PUBLISHED=false
RELEASE_PERFORMED=false
NAS_ACCESSED=false
B2_STARTED=false
DEPLOYMENT_PERFORMED=false
SLICE_4_SPEC=docs/adr/ADR-003-deterministic-offline-game-scoring.md
SLICE_4_EVIDENCE=docs/evidence/arch-001-slice-4-formal-closure.md
SLICE_4_BASELINE_COMMIT=9a789b8ccd9b7067e84e161d8bcd8b1fd954dca4
SLICE_5_SPEC=docs/adr/ADR-004-offline-game-history-and-resume.md
SLICE_5_REQUIREMENTS=ACCEPTED
SLICE_5_IMPLEMENTATION_AUTHORIZED=true
SLICE_5_IMPLEMENTATION_ACCEPTED=true
SLICE_5_FORMALLY_CLOSED=true
SLICE_5_EVIDENCE=docs/evidence/arch-001-slice-5-implementation.md
SLICE_5_AUTOMATED_ACCEPTANCE=PASS
SLICE_5_PHYSICAL_DEVICE_ACCEPTANCE=PASS
SLICE_5_OFFLINE_ACCEPTANCE=PASS
SLICE_5_INTEGRATION_PENDING=false
SLICE_5_PRODUCTION_RELEASE=false
SLICE_5_ACCEPTANCE_TAG=v0.5.0-arch001-slice5
SLICE_6_SPEC=docs/adr/ADR-005-scoring-first-device-shell.md
SLICE_6_REQUIREMENTS=ACCEPTED
SLICE_6_IMPLEMENTATION_AUTHORIZED=true
SLICE_6_IMPLEMENTATION_ACCEPTED=true
SLICE_6_FORMALLY_CLOSED=true
SLICE_6_EVIDENCE=docs/evidence/arch-001-slice-6-implementation.md
SLICE_6_AUTOMATED_ACCEPTANCE=PASS
SLICE_6_PHYSICAL_DEVICE_ACCEPTANCE=PASS
SLICE_6_OFFLINE_ACCEPTANCE=PASS
SLICE_6_LOCAL_BASELINE_COMMIT_PENDING=false
SLICE_6_INTEGRATION_PENDING=false
SLICE_6_ACCEPTANCE_TAG=v0.6.0-arch001-slice6
SLICE_6_FIELD_VALIDATION=PAUSED
FIRST_USE_SPEC=docs/adr/ADR-006-first-use-scoring-presentation.md
FIRST_USE_USABILITY=REQUIREMENTS_ACCEPTED
USER_WALKTHROUGH=ACCEPT
USER_FIRST_USE_DECISION=ACCEPT
USER_REPORTED_OBSTACLES=NONE
COACHING_HISTORY=NOT_CONFIRMED
OVERALL_ACCEPTANCE=HOLD
FIRST_USE_IMPLEMENTATION_AUTHORIZED=true
FIRST_USE_EVIDENCE=docs/evidence/arch-001-first-use-presentation.md
TECHNICAL_VERIFICATION=PASS
FIRST_USE_ACCEPTANCE=ACCEPTED_LOCAL_CANDIDATE_LAYOUT_FOLLOW_ON_TECHNICALLY_VERIFIED
LOCAL_FIRST_USE_PACKAGE_ACCEPTED=true
HISTORY_PLACEMENT_TECHNICALLY_VERIFIED=true
USER_TESTED_FINAL_PLACEMENT=false
FIRST_USE_INTEGRATION_PENDING=false
FIRST_USE_INTEGRATED_ON_DEFAULT_BRANCH=true
PRODUCTION_RELEASE_AUTHORIZED=false
TENTH_FRAME_DEFECT=CLOSED
INVALID_REPAIR_UI=PASS
P2_CONNECTION_CACHE=CLOSED
P2_RECOVERY_COVERAGE=CLOSED
SQLITE_DIAGNOSTIC_DISPOSITION=RETAIN_ON_SHARED_INTEGRATION_NOT_PRODUCTION
PRODUCTION_DIAGNOSTIC_APPROVAL=false
ORIGINAL_NATIVE_CAUSE=UNCONFIRMED
DIRECT_FRAME_CORRECTION_SELECTION=INCLUDED_IN_B1_IMPLEMENTATION_CANDIDATE
PIN_DETAIL_AND_ANALYSIS=B1_IMPLEMENTED_READY_FOR_ACCEPTANCE_REVIEW
PIN_DETAIL_ANALYSIS_SPEC=docs/adr/ADR-007-pin-detail-and-analysis-requirements.md
PIN_DETAIL_ANALYSIS_REQUIREMENTS_STATUS=ACCEPTED
PIN_DETAIL_U1=ACCEPTED_PINFALL_PER_DELIVERY_APP_CALCULATES_TOTALS
PIN_DETAIL_U2=ACCEPTED_PINFALL_THEN_OPTIONAL_STANDING_EXPLICIT_SAVE
PIN_DETAIL_U3=ACCEPTED_FIRST_BALL_LEAVES_SPARES_TRENDS_DEFERRED
ANALYSIS_DEFINITIONS_RECORDED=true
UNKNOWN_PIN_IDENTITY_RULES_RECORDED=true
CORRECTION_INTEGRITY_RECORDED=true
B1_BOUNDARY_DEFINED=true
B1_SCOPE=CAPTURE_AND_DIRECT_FRAME_BALL_FIX_ONLY
ANALYSIS_IMPLEMENTATION_IN_B1=false
PINLEAVE_WRITES_IN_B1=false
B1_IMPLEMENTATION_AUTHORIZED=true
B1_TECHNICAL_CONTRACT=ACCEPTED
B1_TECHNICAL_CONTRACT_SPEC=docs/adr/ADR-008-b1-pinstate-persistence-sync-contract.md
ADR_008_ACCEPTED=true
EXISTING_PINSTATE_SUPPORT=ACTIVATED_GENERIC_STORE_PLUS_INDEX
SCHEMA_CHANGES_REQUIRED=PARTIAL_UNIQUE_INDEX_APPLIED_IN_CODE
SYNC_CONTRACT_CHANGES_REQUIRED=SERVER_PINSTATE_VALIDATION_AND_PULL_ASSOC
ENTITY_OR_PROTOCOL_CHANGES_REQUIRED=PINSTATE_BASIS_ROLL_VERSION
SCHEMA_OR_SYNC_DECISIONS_OUTSTANDING=NONE_ADR_008_ACCEPTED
TENTH_RACK_RULES_VERIFIED=true
EXAMPLE_10_9_1=BALL3_ON_REMAINING_PINS_NOT_FRESH_RACK
SLICE_6_PRIMARY_SURFACE=ACTIVE_GAME_AND_SCORING
SLICE_6_DIAGNOSTIC_HARNESS=COLLAPSED_BY_DEFAULT_NOT_REMOVED
SLICE_6_HISTORY_PRESENTATION=OPEN_ON_DEMAND
SLICE_6_NO_NEW_CANONICAL_FIELDS=true
SLICE_4_IMPLEMENTATION_AUTHORIZED=true
SLICE_4_IMPLEMENTATION_ACCEPTED=true
SLICE_4_FORMALLY_CLOSED=true
PRODUCTION_IMPLEMENTATION_AUTHORIZED=false
DEVICE_ACCEPTANCE_REQUIRED=true
ARCHITECTURE_AUTHORITY_REVIEW=BOWLING_ARCH_001_SLICE_4
ARCHITECTURE_AUTHORITY_RESULT=PASS
DECISION_4-1_TEN_PIN_AUTHORITY=APPROVED
DECISION_4-4_CORRECTION_DEPENDENCY_POLICY=APPROVED
ARCHITECTURAL_DIRECTION=ACCEPTED
SLICE_4_REQUIREMENTS=ACCEPTED_AFTER_REQUIRED_CLARIFICATIONS
SLICE_4_ARCHITECTURE_ACCEPTED=true
TEN_PIN_RULE_AUTHORITY=USBC_PLAYING_RULES
RULE_AUTHORITY=UNITED_STATES_BOWLING_CONGRESS
RULE_SET=USBC_PLAYING_RULES
RULE_AUTHORITY_PINNED=true
RULE_EDITION=2026-2027
SCORING_DOMAIN=AMERICAN_TENPINS
RULE_REFERENCE_IMMUTABLE=true
RULE_REFERENCE_VERIFIED=true
RULE_REFERENCE_BLOCKER=CLOSED
RULE_PROVENANCE=docs/reference/usbc-playing-rules-2026-2027.provenance.md
SOURCE_SHA256=4cb44a2a9d62fdf18957da2db4646e05342eebdc308cc56711a6249ad2cdddec
ZERO_ROLL_STATE=NOT_STARTED
LIFECYCLE_PERSISTED=false
GOLDEN_VECTORS_PINNED=true
LAST_ACCEPTED_RELEASE=NOT_VERIFIED
LAST_ACCEPTED_APPLICATION_COMMIT=d55743ea0bbcfae28755cd2d19bc6d44eecf39de
LAST_ACCEPTED_COMMIT=d82ed963211cd7a80a774b1b816ab10859026e01
TEST_STATUS=EXECUTABLE_GATES_PASS
SYNC_STATUS=CONFLICT_AND_ACCEPTED_LOCAL_WRITES_ATOMIC
MOBILE_STATUS=AUTHORITATIVE_TYPECHECK_PASS
DEPLOYMENT_STATUS=NOT_AUTHORIZED_NOT_PERFORMED
OPEN_P0=0_OBSERVED_AFTER_LIFECYCLE_FIX_NATIVE_CAUSE_UNCONFIRMED
OPEN_P1=0
OPEN_P2=0
CURRENT_BLOCKERS=NONE
NEXT_ACTION=AWAIT_ARCHITECTURE_AUTHORITY_NEXT_GATE
NEXT_AGENT=ChatGPT_ARCHITECTURE_AND_RELEASE_AUTHORITY
LAST_UPDATED=2026-09-24_America/New_York
SLICE_5_GAME_LABEL=LOCAL_CREATION_DATE_AND_TIME
SLICE_5_RESTART_DEFAULT=NEWEST_GAME
SLICE_5_DIAGNOSTIC_HARNESS=RETAIN_EXISTING_CONTROLS
SLICE_5_BOWLER_PROFILE=NOT_REQUIRED
SLICE_3_ACCEPTED=true
INDEPENDENT_REVIEW_VERDICT=HOLD_NOT_CLEAN
FINDING_CLOSURE=THREE_RECORDED_FINDINGS_CLOSED
MERGE_COMMIT=c04d46930b9644a05505615170892754830a0572
MERGE_STYLE=FAST_FORWARD
INTEGRATION_BRANCH=arch/001-domain-sync-foundation
RELEASE_TAG=NOT_CREATED
SLICE_4_ACCEPTANCE_TAG=v0.4.0-arch001-slice4
SLICE_5_ACCEPTANCE_TAG=v0.5.0-arch001-slice5
POST_MERGE_SMOKE=PASS
BOWLING_NEXT_SLICE_PLAN=ARCH-001_SLICE_6_SCORING_FIRST_DEVICE_SHELL
NEXT_SLICE=ARCH-001_SLICE_6_SCORING_FIRST_DEVICE_SHELL
NEXT_SLICE_IMPLEMENTATION_AUTHORIZED=false
FIELD_VALIDATION=PAUSED
SLICE_7_IMPLEMENTATION_AUTHORIZED=false
IMPLEMENTATION_AUTHORIZED=false
NAS_ACCESSED=false
OTHER_PROJECTS_ACCESSED=false
OTHER_PROJECTS_CHANGED=false
```

## Verified candidate and execution evidence

- Worked only in `C:\Users\Drago\Documents\Bowling`. Canonical origin `https://github.com/Drago1068/Bowling.git`.
- On resume the live origin source branch `arch/001-end-to-end-sync` was the expected reviewed candidate `4077c2bf10d883f64559e314f2a5d05fb1769b0f` (tree `38095255d1d596f5a39f71768d77660c0ad4fa1d`). Local checkout had been moved to `arch/001-mobile-persistence`; local `arch/001-end-to-end-sync` had an extra checkpoint `841929e` containing only this status file. The checkpoint was mixed-reset to origin, restoring HEAD `4077c2b` with `PROJECT_STATUS.md` untracked. No other unexpected application changes.
- Live origin default branch is `arch/001-domain-sync-foundation`. It was `d88d067bfa319aad3b31e38566b0433491e997c9` (ancestor of the candidate) before the authorized fast-forward merge. GitHub reports this as the default branch. No release tag was created.
- Node v24.19.0, npm 11.17.0. Existing root and `apps/mobile` lockfiles used; no `npm ci`, no new tooling in the repository, no unrelated upgrades. Server tests used only `postgresql://bowling:bowling@127.0.0.1:55433/bowling` (`bowling-pg-test`, user/database bowling). `--test-concurrency=1` is test isolation for shared-table truncation, not production concurrency proof. No overlapping test processes.

### Independent review (unchanged)

Exactly one independent review was launched against Slice 3 commit 4077c2b relative to parent 8af5a25. It covered only authorized sync/recovery concerns, made no changes, and stopped upon finding P1. Verdict: HOLD, not a completed clean review. This closure does not rewrite that verdict.

Recorded findings at review:

1. **P1: conflict recovery is not atomic** — `src/sync/coordinator.ts` persisted terminal CONFLICT before inserting its conflict record.
2. **P2: accepted state and receipt are not atomic** — ACCEPTED was persisted before its receipt; existing fault injection fired after both writes.
3. **P2: assertion failures leak E2E resources** — `server/test/e2e.sync.test.ts` closed mobiles/pools only on success; file cleanup did not always close handles first.

### Finding closure (subsequent bounded remediation)

Remediation commit `c04d46930b9644a05505615170892754830a0572` (tree `69f005eff08c8991bbc539e4c27c1a8c84b803b9`) is a child of the reviewed source `4077c2b`. Production persistence/outbox/correction/lifecycle/domain contracts were not broadened. Changes:

1. **P1 CLOSED** — CONFLICT outbox state and conflict row commit in one local SQLite transaction. Fault `afterConflictStateBeforeRecord` injects between the writes. Crash rolls back both; restart leaves the submission resumable (`SUBMITTED`); replay of the same submission id produces exactly one durable local conflict record; server still has one conflict row for that submission.
2. **P2 CLOSED** — ACCEPTED, server receipt, and last-server-cursor commit in one local transaction. Fault `afterAcceptedBeforeReceipt` injects between ACCEPTED and the receipt. Crash rolls back both; restart replay uses the same submission id, server returns `ALREADY_ACCEPTED`, then confirms without duplicate canonical/change-feed mutation. Existing post-receipt `afterServerResultBeforeConfirm` recovery remains.
3. **P2 CLOSED** — Slice 3 E2E tests register mobiles/application pools and close them in `finally`. SQLite handles close before deleting `-wal`/`-shm`/db files.

### Pre-merge gates (remediation candidate c04d469)

- Domain/mobile non-UI: `npm test` 98/98 PASS, exit 0.
- Server: `npm run test:server` `--test-concurrency=1` 39/39 PASS, exit 0 (includes 17 E2E tests).
- Shared/server typecheck: `npm run typecheck` PASS, exit 0.
- Mobile typecheck: `npm run typecheck:mobile` PASS, exit 0, against the existing locked mobile dependency installation.
- Lint: NOT_CONFIGURED in root/mobile scripts. No repository CI workflow.
- Five sequential E2E runs: `node --experimental-strip-types --test --test-concurrency=1 server/test/e2e.sync.test.ts` — 17/17 PASS each, 85 executions, 0 failures.
- Focused crash-boundary and cleanup tests: ACCEPTED receipt crash, conflict state crash, and server-restart cleanup path PASS.

### Merge and post-merge smoke

- Fast-forward `arch/001-domain-sync-foundation` to `c04d469` (same commit as the feature branch; no merge commit, reviewed ancestry preserved).
- Post-merge on default: domain 98/98 PASS; server 39/39 PASS; mobile typecheck PASS; E2E smoke 17/17 PASS.

## Forward planning boundary

`CURRENT_GATE=FIRST_USE_HOLD_TENTH_FRAME_THREE_STRIKE`.
Slice 4 remains formally closed on `9a789b8` / `v0.4.0-arch001-slice4`.
Slice 5 remains formally closed on `e45358d` /
`v0.5.0-arch001-slice5`. Slice 6 is integrated on live
`arch/001-domain-sync-foundation` at `94cfc4b` / tree `4582a137`, tagged
`v0.6.0-arch001-slice6` (annotated tag object `da8894a5`). That tag is an
acceptance marker, not a production store release. Historical HOLDs
(`ADB_AND_MOBILE_ACCEPTANCE_ENABLEMENT`, `DEBUG_ARTIFACT_REQUIRES_METRO`,
Slice 6 locked-install) remain on the record. Slice 6 technical/device
PASS is not erased. First-use presentation is implemented on
`codex/first-use-scoring-presentation` (uncommitted). Technical verification
of the shell remains PASS. The actual user understood start/entry/correct/
reopen; overall first-use is **HOLD** because tenth-frame three-strike
entry fails. Field validation remains **paused**.
`NEXT_ACTION=BOUNDED_TENTH_FRAME_FILL_LEGALITY_REMEDIATION`.
`SLICE_7_IMPLEMENTATION_AUTHORIZED=false`.
The Slice 6 local `assembleRelease` APK SHA-256
`406ccca87c1e0d1a5641934fc0e7b78fd8a3fa7a77f414cbc23abd2435bb2a70`
is an acceptance artifact, not a production store release.
The earlier debug-APK force-stop/relaunch HOLD (`DEBUG_ARTIFACT_REQUIRES_METRO`)
and the Slice 4 initial ADB device HOLD remain on the record and are not
rewritten as earlier PASS. The local `assembleRelease` / `export:embed`
acceptance APK (SHA-256
`0038f8f8a543ce95c77d08b43b0ffd57ef63abcde6eb87b42e4b571778e2bca7`) is not a
production store release.

### Slice 4 architecture acceptance (APPROVED requirements; implementation unauthorized)

- **Specification**: `docs/adr/ADR-003-deterministic-offline-game-scoring.md`
  (Accepted requirements/design after required clarifications).
- **Work package**: `BOWLING_ARCH_001_SLICE_4_ARCHITECTURE_ACCEPTANCE_CLOSURE`.
- **Architecture Authority review**: `BOWLING_ARCH_001_SLICE_4` / `RESULT=APPROVED`.
- **Decision 4-1**: APPROVED — `RULE_AUTHORITY=USBC_PLAYING_RULES`,
  `RULE_EDITION=2026-2027`, `SCORING_DOMAIN=AMERICAN_TENPINS`. Official artifact
  from `https://bowl.com/rules` →
  `usbc-playing-rulebook-26-27.pdf`, SHA-256
  `4cb44a2a9d62fdf18957da2db4646e05342eebdc308cc56711a6249ad2cdddec`.
  Provenance: `docs/reference/usbc-playing-rules-2026-2027.provenance.md`.
  Publication not committed (USBC copyright). `RULE_REFERENCE_BLOCKER=CLOSED`.
  Future USBC editions do not mutate existing derived results.
- **Decision 4-4**: APPROVED —
  `CORRECTION_DEPENDENCY_POLICY=PRESERVE_FACTS_AND_EXPOSE_DOMAIN_INVALID_REQUIRING_REPAIR`.
- **Zero-roll lifecycle**: `GAME_IDENTITY_MAY_EXIST_WITH_ZERO_ROLL_FACTS=true`;
  zero-roll derived state is `NOT_STARTED`; first effective roll derives
  `IN_PROGRESS`; legally complete fact set derives `COMPLETED`;
  `LIFECYCLE_FIELD_PERSISTED=false`. No manufactured creation roll.
- **Golden vectors pinned** (totals unchanged): `PERFECT_GAME=300` sequence
  `10×12`; `ALL_SPARES=150` sequence `5×21`; `GUTTER_GAME=0` sequence `0×20`;
  `OPEN_FRAMES=80` sequence `(5,3)×10`; `MIXED_GAME=169` sequence
  `10,3,6,9,1,7,2,10,10,8,2,9,0,10,7,3,9`.
- **Scope preserved**: `OBSERVED_ROLL_FACTS=AUTHORITATIVE`,
  `DERIVED_SCORE=NON_AUTHORITATIVE`; frame identity authoritative / scoring
  semantics derived; existing append-only corrections; Slice 3 sync frozen.
- **Baseline unmodified**: these documentation changes do not touch the accepted
  application baseline `c04d469` bytes, migrations, dependencies, or sync
  contracts. Slice 3 independent review verdict (HOLD) and closure evidence are
  preserved exactly (unchanged, not rewritten).
- **Recorded against**: status-record commit `01145638f36066cbb62c0d729e1b2f0fb09319f1`
  plus this documentation acceptance commit.
- **Forbidden-change verification for this work package**: `PRODUCTION_CODE_CHANGED=false`,
  `MIGRATIONS_CHANGED=false`, `DEPENDENCIES_CHANGED=false`,
  `SYNC_CONTRACTS_CHANGED=false`, `NAS_ACCESSED=false`,
  `OTHER_PROJECTS_ACCESSED=false`, `OTHER_PROJECTS_CHANGED=false`.
- **Next action**: `RETURN_TO_ARCHITECTURE_AUTHORITY_FOR_SLICE_4_IMPLEMENTATION_AUTHORIZATION`.

### Slice 4 rule-authority pinning (CLOSED; implementation still unauthorized)

- **Work package**: `BOWLING_ARCH_001_SLICE_4_RULE_AUTHORITY_PINNING`.
- **Pinned edition**: USBC Playing Rules `2026-2027` (American Tenpins).
- **Authoritative source**: official BOWL.com 2026–2027 rulebook PDF.
- **SHA-256 verified** before commit:
  `4cb44a2a9d62fdf18957da2db4646e05342eebdc308cc56711a6249ad2cdddec`.
- **Recorded against**: Slice 4 architecture acceptance commit
  `67f91ebe864bdb5d5018e73f91809850272bd5b2` plus this pinning commit.
- **Forbidden-change verification for this work package**: `PRODUCTION_CODE_CHANGED=false`,
  `MIGRATIONS_CHANGED=false`, `DEPENDENCIES_CHANGED=false`,
  `SYNC_CONTRACTS_CHANGED=false`, `NAS_ACCESSED=false`,
  `OTHER_PROJECTS_ACCESSED=false`, `OTHER_PROJECTS_CHANGED=false`.

### Slice 4 implementation, P1 remediation, and device acceptance (historical then closed)

These events remain on the record. HOLDs are not rewritten as if they did not occur.

- **Implementation** `13c2da002fbab72a7dd63ab0c9363ff06a5f1477` — deterministic
  one-bowler offline ten-pin scoring against ADR-003.
- **P1 finding (historical)**: `SLICE4-P1-CORRECTION-DOWNSTREAM-LEGALITY` —
  leftover second-ball facts after a topology-changing correction must not be
  flattened into strike bonuses. **Final status: CLOSED** on
  `9fbb6f8f61955a2ee0d775b685295e1d4017566d`.
- **Initial device acceptance: HOLD** at `9fbb6f8` —
  `INITIAL_DEVICE_HOLD_REASON=ADB_AND_MOBILE_ACCEPTANCE_ENABLEMENT` (ADB not
  `device`; thin UI did not expose the acceptance path).
- **Mobile enablement** `b84646b169e32375131d417cd20c97b74fc49a6c` closed the
  acceptance-flow finding; device RSA authorization remained open.
- **Native Android enablement and final device PASS**
  `d55743ea0bbcfae28755cd2d19bc6d44eecf39de` on Samsung SM-S936U (Android 16,
  serial `R3CY40E6FVJ`), package `com.drago1068.bowling`, **debug** APK (not a
  production release).

### Slice 4 formal closure (this work package)

- **Work package**: `BOWLING_ARCH_001_SLICE_4_FORMAL_CLOSURE`.
- **Evidence**: `docs/evidence/arch-001-slice-4-formal-closure.md`.
- **Architecture Authority**: `RESULT=PASS`;
  `SLICE_4_IMPLEMENTATION_ACCEPTED=true`; `SLICE_4_FORMALLY_CLOSED=true`;
  `SLICE_5_IMPLEMENTATION_AUTHORIZED=false`.
- **Automated**: `ROOT_TESTS=141/141_PASS`, `SERVER_TESTS=39/39_PASS`,
  `TYPECHECK=PASS`, `MOBILE_TYPECHECK=PASS`, `SLICE_3_REGRESSION=PASS`.
  Goldens: 300 / 150 / 0 / 80 / 169.
- **Acceptance tag** (annotated, on this closure commit):
  `v0.4.0-arch001-slice4`.
- **Not performed**: merge, production release, deployment, NAS access,
  other projects, Slice 5.

### Slice 5 requirements acceptance (implementation unauthorized)

- **Work package**: `BOWLING_SLICE_5_REQUIREMENTS_ACCEPTANCE`.
- **Specification**: `docs/adr/ADR-004-offline-game-history-and-resume.md`.
- **Against verified Slice 4 baseline**: `9a789b8` /
  `v0.4.0-arch001-slice4`.
- **Product decisions**: `GAME_LABEL=LOCAL_CREATION_DATE_AND_TIME`,
  `RESTART_DEFAULT=NEWEST_GAME`,
  `DIAGNOSTIC_HARNESS=RETAIN_EXISTING_CONTROLS`,
  `BOWLER_PROFILE=NOT_REQUIRED`.
- **In-session**: explicit open remains selected until user opens another or
  creates a new game. Restart opens newest persisted game; no last-opened
  persistence.
- **Implementation**: awaiting a separate bounded authorization gate.
- **Forbidden for this documentation package**: production code, tests,
  migrations, dependencies, commits, merge, push, tag, NAS, other projects.

### Slice 5 bounded implementation (not formally accepted)

- **Work package**: `BOWLING_SLICE_5_BOUNDED_IMPLEMENTATION`.
- **Branch**: `codex/arch-001-slice-5-history-resume` (uncommitted).
- **Evidence**: `docs/evidence/arch-001-slice-5-implementation.md`.
- **Against verified Slice 4 baseline**: `9a789b8` /
  `v0.4.0-arch001-slice4`.
- **Automated**: `ROOT_TESTS=150/150_PASS`, `SERVER_TESTS=39/39_PASS` on
  existing `bowling-pg-test-tmp` / `127.0.0.1:15433` (stopped afterward;
  volume preserved), `TYPECHECK=PASS`, `MOBILE_TYPECHECK=PASS`.
- **Physical device**: `HOLD` — Samsung `R3CY40E6FVJ` `adb device`,
  data-preserving debug update, offline flows observed except
  force-stop/relaunch visual confirmation. See evidence file.
- **Not claimed**: formal acceptance, closure, commit, merge, tag, release.

### Slice 5 device acceptance completion (not formal acceptance)

- **Work package**: `BOWLING_SLICE_5_DEVICE_ACCEPTANCE_COMPLETION`.
- **ADB**: `R3CY40E6FVJ` `device`; airplane mode on during observed flows.
- **Install**: `adb install -r`; `firstInstallTime` unchanged `2026-09-07`.
- **Observed PASS**: two labeled games, switch, resume incomplete, correct
  prior, new game preserves older, in-session selection.
- **HOLD**: force-stop/relaunch did not restore a dumpable scoring UI (Metro
  debug JS). Newest-after-restart not visually confirmed.

### Slice 5 offline restart diagnosis (not remediation)

- **Work package**: `BOWLING_SLICE_5_OFFLINE_RESTART_DIAGNOSIS`.
- **Classification**: `DEBUG_ARTIFACT_REQUIRES_METRO`.
- Debug APK has no `index.android.bundle`. Without Metro,
  `Unable to load script` / `loadJSBundleFromAssets`. Process launches COLD.
- SQLite file/WAL remained; with Metro, UI opened newest game `01a08e15`
  (diagnostic only, not offline acceptance).
- **Proposed (not executed in that package)**: `assembleRelease` embed via
  existing `export:embed`, same package/debug signing, `adb install -r`, then
  offline force-stop/relaunch **without** Metro.
- **Not claimed in that package**: formal acceptance, closure, commit, merge,
  tag, release.

### Slice 5 self-contained APK and offline retest (not formal acceptance)

- **Work package**: `BOWLING_SLICE_5_SELF_CONTAINED_DEVICE_ACCEPTANCE`.
- **Candidate**: unchanged Slice 5 application hashes on `9a789b8` working tree.
- **Artifact**: existing `assembleRelease` / `export:embed`; APK SHA-256
  `0038f8f8a543ce95c77d08b43b0ffd57ef63abcde6eb87b42e4b571778e2bca7`;
  nonempty `assets/index.android.bundle`; package `com.drago1068.bowling`
  `0.1.0` / `1`; signing certificate SHA-256
  `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`
  matched the installed app. Not a production/store release.
- **Install**: `adb install -r`; `firstInstallTime` still
  `2026-09-07 12:13:15`; `lastUpdateTime=2026-09-11 18:35:16`.
- **Offline**: airplane on; Wi-Fi and cellular disabled during retest; USB
  reverse `tcp:8081` removed; unrelated reverses restored (`8091`, `8000→8011`).
- **Observed PASS**: usable scoring/history UI after force-stop/relaunch
  without Metro; newest game after install was `01a08e15`; older games
  remained; switching to `01a08d84` then restart selected `01a08e15`; compact
  list/switch, resume `F1 1,2`, correct `01a08d85` `8→7`, new game
  `01a092a6` preserving older rows; restart after new game selected
  `01a092a6`.
- **Not claimed**: formal acceptance, closure, commit, merge, tag, production
  release.

### Slice 5 formal acceptance and local baseline (this package)

- **Work package**: `BOWLING_SLICE_5_ACCEPTANCE_AND_LOCAL_COMMIT`.
- **Architecture Authority**: conditional approval verified against the
  recorded candidate hashes, automated evidence, offline device evidence, and
  acceptance APK SHA-256
  `0038f8f8a543ce95c77d08b43b0ffd57ef63abcde6eb87b42e4b571778e2bca7`.
- **Result**: `SLICE_5_IMPLEMENTATION_ACCEPTED=true`,
  `SLICE_5_FORMALLY_CLOSED=true`, `AUTOMATED_ACCEPTANCE=PASS`,
  `PHYSICAL_DEVICE_ACCEPTANCE=PASS`, `OFFLINE_ACCEPTANCE=PASS`,
  `INTEGRATION_PENDING=true`, `PRODUCTION_RELEASE=false`,
  `SLICE_6_IMPLEMENTATION_AUTHORIZED=false`.
- **Acceptance APK**: local `assembleRelease` artifact only; not a production
  store release.
- **Historical HOLDs preserved**: Slice 4 initial
  `ADB_AND_MOBILE_ACCEPTANCE_ENABLEMENT`; Slice 5 debug
  `DEBUG_ARTIFACT_REQUIRES_METRO` (resolved by the later self-contained APK
  retest, not rewritten as an earlier PASS).
- **This package**: one local feature-branch commit; no merge, push, tag,
  production release, or Slice 6 authorization.

### Slice 5 integration and marker (historical; current state noted above)

Live default `arch/001-domain-sync-foundation` was later fast-forwarded to
`e45358d` and tagged `v0.5.0-arch001-slice5`. That publication is not
rewritten here as if it had already occurred in the local-baseline package.

### Slice 6 requirements acceptance (historical; implementation later authorized)

- **Work package**: `BOWLING_SLICE_6_REQUIREMENTS_ACCEPTANCE`.
- **Specification**: `docs/adr/ADR-005-scoring-first-device-shell.md`.
- **Against verified Slice 5 baseline**: `e45358d` / `v0.5.0-arch001-slice5`.
- **Product decisions**: `PRIMARY_SURFACE=ACTIVE_GAME_AND_SCORING`,
  `DIAGNOSTIC_HARNESS=COLLAPSED_BY_DEFAULT_NOT_REMOVED`,
  `HISTORY_PRESENTATION=OPEN_ON_DEMAND`, `NO_NEW_CANONICAL_FIELDS=true`.
- **This documentation package** did not implement application code.

### Slice 6 bounded implementation (historical; device HOLD at the time)

- **Work package**: `BOWLING_SLICE_6_BOUNDED_IMPLEMENTATION`.
- **Working branch**: `codex/arch-001-slice-6-scoring-first-shell` parent
  `e45358d` / tree `f40749ae`. No commit.
- **Evidence**: `docs/evidence/arch-001-slice-6-implementation.md`.
- **Automated**: root `153/153` PASS (+3 disclosure tests vs Slice 5 `150`);
  server `39/39` PASS on `bowling-pg-test-tmp` `127.0.0.1:15433` then stopped,
  volume preserved; both typechecks PASS.
- **Acceptance APK**: SHA-256
  `406ccca87c1e0d1a5641934fc0e7b78fd8a3fa7a77f414cbc23abd2435bb2a70`;
  embedded JS present; package `com.drago1068.bowling` `0.1.0`/`1`; signing
  certificate matched the installed app. Not a production/store release.
- **Physical / offline at that package**: `HOLD` —
  `DEVICE_LOCKED_BLOCKING_DATA_PRESERVING_INSTALL` on Samsung `R3CY40E6FVJ`.
  Two `adb install -r` attempts hung and were killed. That HOLD is not
  rewritten as an earlier PASS.
- **Historical HOLDs preserved**: Slice 4 initial
  `ADB_AND_MOBILE_ACCEPTANCE_ENABLEMENT`; Slice 5 debug
  `DEBUG_ARTIFACT_REQUIRES_METRO`.

### Slice 6 device acceptance completion (this package)

- **Work package**: `BOWLING_SLICE_6_DEVICE_ACCEPTANCE_COMPLETION`.
- **Candidate**: unchanged vs automated package (same four source SHA-256
  values and APK SHA-256); no rebuild; automated gates not rerun.
- **Install**: pulled installed `base.apk` matched
  `406ccca87c1e0d1a5641934fc0e7b78fd8a3fa7a77f414cbc23abd2435bb2a70`.
  `lastUpdateTime=2026-09-11 21:42:38`; `firstInstallTime` still
  `2026-09-07 12:13:15`. No second `adb install -r`. No uninstall/clear.
- **Physical / offline**: `PASS` on `R3CY40E6FVJ` without Metro. Scoring
  primary; History 30 games including Sep 7–8 (listed only); resume
  `01a08d85` `F1 7` / 2 corrections; acceptance game `01a093b9` record 4
  correct to 3; diagnostics collapsed by default and fully reachable;
  force-stop/relaunch selected `01a093b9`. Layout screenshots recorded.
- **Not claimed**: formal acceptance, commit, merge, push, tag, production
  release, or Slice 7.
- **Confirmatory retest 2026-09-12**: same APK already installed; no
  rebuild or `adb install -r`; ADR-005 gates repeated offline without a
  new game; `PASS`. Hung-install HOLD preserved.

### Slice 6 implementation acceptance (this package; commit pending)

- **Work package**: `BOWLING_SLICE_6_ACCEPTANCE_DECISION`.
- **Result**: `SLICE_6_IMPLEMENTATION_ACCEPTED=true`,
  `AUTOMATED_ACCEPTANCE=PASS`, `PHYSICAL_DEVICE_ACCEPTANCE=PASS`,
  `OFFLINE_ACCEPTANCE=PASS`, `LOCAL_BASELINE_COMMIT_PENDING=true`.
- **Candidate**: parent `e45358d` / tree `f40749ae`; working-tree hashes
  unchanged vs automated and device packages; APK SHA-256
  `406ccca87c1e0d1a5641934fc0e7b78fd8a3fa7a77f414cbc23abd2435bb2a70`.
- **Hung installs**: two terminated `adb install -r` attempts remain HOLD;
  later pulled `base.apk` matched the built artifact. Neither hung command
  is recorded as `Success`.
- **Not performed**: merge, push, tag, production release, or Slice 7.

### Slice 6 local baseline commit (this package)

- **Work package**: `BOWLING_SLICE_6_LOCAL_BASELINE_COMMIT`.
- **Result**: Slice 6 locally closed on the feature branch. Integration and
  publication pending. No production release. Slice 7 unauthorized.
- **Hung installs**: preserved as historical HOLD; later matching APK
  install verified by pulled `base.apk`, not by treating terminated ADB
  commands as `Success`.

### Slice 6 field-validation preparation (historical; later paused)

- **Work package**: `BOWLING_SLICE_6_FIELD_VALIDATION_PREPARATION`.
- **Protocol**: `docs/validation/slice-6-field-validation.md`.
- **Result at preparation**: `FIELD_VALIDATION=PREPARED_NOT_EXECUTED`. No
  game was bowled. That preparation is not rewritten as executed.
- **Verified current baseline**: live `arch/001-domain-sync-foundation`
  `94cfc4b` / `v0.6.0-arch001-slice6`. Not a production release.
- **Later**: real-lane validation paused for first-use usability (below).
- **Not authorized in that package**: implementation, Slice 7, NAS.

### First-use usability requirements (historical; implementation later authorized)

- **Work package**: `BOWLING_FIRST_USE_REQUIREMENTS_ACCEPTANCE`.
- **Specification**: `docs/adr/ADR-006-first-use-scoring-presentation.md`.
- **Against verified Slice 6 baseline**: `94cfc4b` /
  `v0.6.0-arch001-slice6`.
- **User report**: interface not understandable. Treated as a usability
  blocker, not a scoring FAIL and not a completed field FAIL.
- **Result**: `FIRST_USE_USABILITY=REQUIREMENTS_ACCEPTED`,
  `USER_WALKTHROUGH=PENDING`, `FIELD_VALIDATION=PAUSED`,
  `IMPLEMENTATION_AUTHORIZED=false` at that package.
- **This documentation package** did not change application code.

### First-use presentation implementation (this package; uncommitted)

- **Work package**: `BOWLING_FIRST_USE_PRESENTATION_IMPLEMENTATION`.
- **Branch**: `codex/first-use-scoring-presentation` parent `94cfc4b`.
- **Evidence**: `docs/evidence/arch-001-first-use-presentation.md`.
- **Automated**: root `156/156` PASS; server `39/39` PASS on
  `bowling-pg-test-tmp` `127.0.0.1:15433` then stopped, volume preserved;
  both typechecks PASS.
- **APK**: SHA-256
  `ce47168678436d5ce22eda357da98a0cf0bcc8061294c16c4005f06b9755c49c`;
  installed `base.apk` matched; `adb install -r` Success; firstInstallTime
  still `2026-09-07 12:13:15`. Not a production/store release.
- **Technical device**: PASS on dedicated 10:58:50 PM practice game
  (enter/save/fix/cancel/correct/history Open/Advanced/restart-newest).
  Completed-game and invalid-repair screens were not exercised on device.
- **Result**: `TECHNICAL_VERIFICATION=PASS`, `USER_WALKTHROUGH=PENDING`,
  `FIRST_USE_ACCEPTANCE=NOT_ESTABLISHED`, `FIELD_VALIDATION=PAUSED`.
- **Handoff**: app left open on the 10:58:50 PM practice game, Frame 1
  2nd ball. Existing user games preserved. No commit, push, tag, or NAS.
- **Not claimed**: user comprehension, field validation, production release.

### Tenth-frame three-strike diagnosis (this package; no code fix)

- **Work package**: `BOWLING_TENTH_FRAME_THREE_STRIKE_DIAGNOSIS`.
- **User feedback**: app understood; start simple; ordinary entry OK;
  tenth-frame three strikes do not work; correction OK (wants tap-on-frame);
  reopen OK; wants more functionality and pin detail (undefined).
- **Result**: `OVERALL_ACCEPTANCE=HOLD`, `USER_WALKTHROUGH=HOLD`,
  `FIELD_VALIDATION=PAUSED`, `RESULT=REPRODUCED`.
- **Reproduction**: new game `9/12/2026, 11:50:05 PM`; nine strikes then
  F10 first-ball 10. Second-ball 1–10 disabled (only 0). Tap 10 did not
  save. User game `11:34:32 PM` / 172 not modified.
- **Classification**: `validateNextRoll` tenth-frame ball-2 sum check after
  a first-ball strike, surfaced by the entry pad via `pinfallLegal`.
  `deriveGame` perfect-game facts still 300; that test does not prove UI
  entry. `shellPresentation.ts` remains presentation-only.
- **Not performed at diagnosis**: remediation, rebuild, install, commit, NAS.
- **Deferred**: direct frame correction selection; pin-detail requirements.

### Tenth-frame legality remediation (this package; install HOLD)

- **Work package**: `BOWLING_TENTH_FRAME_LEGALITY_REMEDIATION`.
- **Production change**: `src/scoring/validate.ts` only. Tenth-frame ball 2
  after a first-ball strike is 0–10; open-rack sum still applies when the
  first ball is below 10; third-ball legality follows ADR-003 (not 0–10 for
  every third ball). `deriveGame`, identity, correction audit, persistence,
  outbox, and stored observations unchanged.
- **Tests**: `recordRoll` / entry-legality path in
  `tests/mobile.scoring-flow.test.ts` plus `validateNextRoll` cases in
  `tests/scoring.test.ts`. Root `163/163` PASS; server `39/39` PASS on
  existing `bowling-pg-test-tmp` `127.0.0.1:15433` then stopped, volume
  preserved; both typechecks PASS. Rejected rolls leave facts/outbox
  unchanged in those tests.
- **APK**: rebuilt SHA-256
  `3e9ecef1ffb6699da41f566c6e9ab4ed5bd2908ecc1d7897f07a693f00e7db43`
  (prior `ce471686…` does not cover the fix). Signing matches installed
  package. Not a production/store release.
- **Device**: `HOLD` —
  `DEVICE_LOCKED_BLOCKING_DATA_PRESERVING_INSTALL` on `R3CY40E6FVJ`. One
  `adb install -r` hung (Play Protect behind lock/Bouncer) and was killed.
  That hung command is not recorded as `Success`. `firstInstallTime`
  `2026-09-07 12:13:15`.
- **Result**: `OVERALL_ACCEPTANCE=HOLD`, `USER_WALKTHROUGH=HOLD`,
  `FIELD_VALIDATION=PAUSED`, `FIRST_USE_ACCEPTANCE=NOT_ESTABLISHED`.
- **Not performed**: commit, merge, push, tag, NAS, field testing.

### Tenth-frame three-strike diagnosis (this package; no code change)

- **Work package**: `BOWLING_TENTH_FRAME_THREE_STRIKE_DIAGNOSIS`.
- **User feedback recorded**: understands app; start simple; ordinary entry
  OK except tenth-frame three strikes; correction OK (wants tap-on-frame);
  reopen OK; more functionality / pin detail (undefined).
- **Result**: `BLOCKED` for a live UI sequence this morning. Device
  `R3CY40E6FVJ` stayed on the lock screen. No new game. No DB patches. No
  install/rebuild. `APPLICATION_CODE_CHANGED=false`.
- **APK identity**: expected diagnosis SHA `ce471686…` is **not** what is
  installed. Pulled `base.apk` SHA-256
  `3e9ecef1ffb6699da41f566c6e9ab4ed5bd2908ecc1d7897f07a693f00e7db43`
  (`lastUpdateTime=2026-09-13 01:37:47`). Bundle contains `"fill balls"`.
  How that update landed is not claimed by this package.
- **Historical reproduction preserved**: 2026-09-12 game `11:50:05 PM` on
  `ce471686…` — after F10 first-ball 10, only 0 enabled; second strike did
  not save. `PERFECT_GAME` facts still 300 and do not prove UI `recordRoll`.
  Baseline `validateNextRoll` at `94cfc4b` sums tenth ball 2 with a first-
  ball strike. Pad uses `pinfallLegal`. `shellPresentation.ts` remains
  presentation-only.
- **Deferred**: `DIRECT_FRAME_CORRECTION_SELECTION`;
  `PIN_DETAIL_REQUIREMENTS`.
- **Remaining gates at that diagnosis**: unlock and offline-verify dedicated
  tenth-frame games (closed by the verification package below);
  invalid-repair UI. `OVERALL_ACCEPTANCE=HOLD`, `FIELD_VALIDATION=PAUSED`.

### Installed tenth-frame fix verification (this package)

- **Work package**: `BOWLING_INSTALLED_TENTH_FRAME_FIX_VERIFICATION`.
- **Identity**: pulled `base.apk` SHA-256
  `3e9ecef1ffb6699da41f566c6e9ab4ed5bd2908ecc1d7897f07a693f00e7db43`
  matches the remediating build and working-tree APK. Source hashes for
  `validate.ts` and entry tests unchanged. `ce471686…` remains historical.
  Hung installer HOLD preserved. No rebuild/install this package.
- **Typecheck**: root and mobile PASS this package. Root `163/163` and
  server `39/39` reused (hashes unchanged; suites not rerun; no Postgres).
- **Device**: unlocked `R3CY40E6FVJ`. Wifi disabled for the run; no Bowling
  Metro `8081` reverse. Dedicated **8:10:29 AM** twelve strikes → completed
  **300**, pad gone. Dedicated **8:15:39 AM** F10 `10,9` then only 0/1
  enabled; 1 completes **289**. Force-stop/relaunch and Open preserved both
  finals. User **11:34 PM** / **11:50 PM** listed, not edited.
- **Completed-state UI**: observed (`Game finished. Final score 300.` /
  `289.`). Invalid-repair UI still not exercised.
- **Result**: `READY_FOR_ACCEPTANCE_REVIEW`. `OVERALL_ACCEPTANCE=HOLD`,
  `USER_WALKTHROUGH=HOLD`, `FIELD_VALIDATION=PAUSED`. No commit.

### First-use acceptance gate reconciliation (this package; docs only)

- **Work package**: `BOWLING_FIRST_USE_ACCEPTANCE_GATE_RECONCILIATION`.
- **Candidate**: hashes still match remediating `validate.ts` /
  `3e9ecef1…` APK. HEAD `94cfc4b`.
- **Identity shells `903380` / `903381`**: captured hashes, serial, pulled
  APK SHA, unlock, and package times, then **status=failed** (no exit_code
  footer). That exit is **not** recorded as PASS. Output is sufficient to
  attach later device games to `3e9ecef1…`.
- **Later device script**: 300 / 10,9 / restart-open PASS in `results.json`;
  subsequent console `charmap` exit 1 is not a device FAIL.
- **Tenth-frame defect**: original FAIL on `ce471686…` preserved;
  **CLOSED** after `3e9ecef1…` verification.
- **PASS**: automated 163/163 and 39/39 (not rerun after remediation);
  typechecks rerun; completed-state UI; restart persistence on acceptance
  games; user games 11:34 / 11:50 preserved.
- **HOLD**: invalid-repair UI (never shown); user walkthrough (recorded
  observations, not a PASS; no invented assistance); deferred frame-tap
  correction and undefined pin detail; overall first-use; field validation
  paused.
- **Result**: `HOLD`. `OVERALL_ACCEPTANCE` remains not established. No
  source/tests/device/commit.

### Invalid-repair device acceptance (this package)

- **Work package**: `BOWLING_INVALID_REPAIR_DEVICE_ACCEPTANCE`.
- **Identity**: local APK still `3e9ecef1…`; installed
  `lastUpdateTime=2026-09-13 01:37:47`; device unlocked. No APK pull (prior
  hung identity shells not repeated). No rebuild/install/source change.
- **Sequence** (ordinary controls; class of `P1 OPEN_TO_STRIKE`): dedicated
  **9/13/2026, 9:12:25 AM** record 3,4; Fix a ball 3→10; both facts remain
  (`10` and `4`); ADR-006 repair copy; no finished score; next pad hidden;
  Fix a ball reachable; append-only 10→3 restores open 3,4 / 7.
- **INVALID_REPAIR_UI=PASS**. Agent-operated, not user comprehension.
- **Preserved**: `TENTH_FRAME_DEFECT=CLOSED`, `USER_WALKTHROUGH=HOLD`,
  overall first-use not established, field validation paused.

### Database open failure diagnosis (this package; no remediation)

- **Work package**: `BOWLING_DATABASE_OPEN_FAILURE_DIAGNOSIS`.
- **Authorization**: diagnosis and evidence only. No uninstall, data/cache
  clear, SQLite delete/rename, forced migration, rebuild, install, commit,
  NAS, or pin-detail implementation.
- **Identity**: branch `codex/first-use-scoring-presentation`, HEAD
  `94cfc4b`. Local release APK SHA-256 still
  `3e9ecef1ffb6699da41f566c6e9ab4ed5bd2908ecc1d7897f07a693f00e7db43`.
  Device `R3CY40E6FVJ` unlocked. Installed package
  `lastUpdateTime=2026-09-13 01:37:47`, `firstInstallTime=2026-09-07 12:13:15`,
  codePath unchanged. **This package did not re-pull `base.apk`.** Installed
  bytes are inferred from that timestamp/path matching the last pull that
  hashed `3e9ecef1…`; they were not rehashed now.
- **Emission**: UI title is startup status `DATABASE_OPEN_FAILED`. App
  `runInit` else-branch (not the `Open failed:` catch). Underlying message
  is Expo `NativeDatabase.execSync` rejected, caused by
  `java.lang.NullPointerException`. `initializeApplication` maps thrown
  SQL after a successful `openDriver()` (including `prepareDatabase`
  pragmas/migrations and later reads) onto `DATABASE_OPEN_FAILED`; a
  structured `DATABASE_MIGRATION_FAILED` is only returned when migrate
  returns `ok: false` without throwing.
- **Reproduction**: one HOT `am start` of already-running pid. Activity
  brought to front still showing the banner. Screenshot
  `%TEMP%\db-open-fail\01-launch.png`. Cold-start recurrence not separately
  exercised (one-launch limit). Filtered logcat had no Bowling-owned SQLite
  stack; storage `/data` not full (~75G free). `run-as` denied (release, not
  debuggable). Database, WAL, and SHM were **not listed or copied**.
- **Not claimed**: file corruption; game contents preserved or lost.
  Historical 300 / 289 / invalid-repair / 11:34 / 11:50 tests remain
  historical and do not override this launch failure.
- **Result**: `DIAGNOSED`. `DATABASE_STATE=INACCESSIBLE`.
  `OVERALL_ACCEPTANCE=HOLD`. `FIELD_VALIDATION=PAUSED`.
  `APPLICATION_CODE_CHANGED=false`. `COMMITS_CREATED=false`.
- **Pin-detail / analysis**: recorded as a proposed requirement only
  (`PIN_DETAIL_AND_ANALYSIS_REQUEST=RECORDED_NOT_AUTHORIZED`). Unresolved
  items listed in first-use evidence. No schema/UI/scoring change.

### Database cold-start recovery (this package; no code change)

- **Work package**: `BOWLING_DATABASE_COLD_START_RECOVERY`.
- **Identity**: HEAD `94cfc4b`. Pulled installed `base.apk` SHA-256
  `3e9ecef1ffb6699da41f566c6e9ab4ed5bd2908ecc1d7897f07a693f00e7db43`
  (`INSTALLED_APK_IDENTITY=VERIFIED`). Matches local release APK and
  `LAST_KNOWN_APK_SHA256`. `lastUpdateTime` still `2026-09-13 01:37:47`.
- **Cold start**: device unlocked; pre-stop UI was `DATABASE_OPEN_FAILED`
  with the `execSync` NPE note (not mid-entry). Logs preserved (no
  `logcat -c`). Force-stop `com.drago1068.bowling` then one
  `am start -W` `LaunchState=COLD` (new pid 31963). Banner absent.
  Newest game restored: **9/13/2026, 9:12:25 AM**, F1 `3 4` / 7, Frame 2
  1st-ball pad. No `NativeDatabase.execSync` NPE in that pid’s logcat.
- **Readable games (labels, not durable IDs)**: 11:34:32 PM finished
  **172**; 11:50:05 PM still F10 2nd ball after F10 first-ball X (nine
  prior strikes); 8:10:29 AM finished **300**; 8:15:39 AM finished **289**;
  9:12:25 AM repair-test as above. Other listed labels were not opened.
  **Not** full-database integrity.
- **Result**: `RECOVERED_THIS_LAUNCH`. `ROOT_CAUSE_CONFIRMED=false`.
  `RECURRENCE_RISK=UNRESOLVED`. Does not prove a stale-handle diagnosis.
  `OVERALL_ACCEPTANCE=HOLD`. `FIELD_VALIDATION=PAUSED`.
  `PIN_DETAIL_AND_ANALYSIS=DEFERRED_REQUIREMENTS`.
  `APPLICATION_CODE_CHANGED=false`. `DEVICE_DATA_RESET=false`. No copy,
  rebuild, install, commit, NAS.

### Lifecycle recurrence check (this package; no code change)

- **Work package**: `BOWLING_LIFECYCLE_RECURRENCE_CHECK`.
- **Identity**: HEAD `94cfc4b`. `lastUpdateTime` / `codePath` unchanged from
  the verified `3e9ecef1…` pull. Not rehashed (identity not changed).
- **Code (observation, not a claimed root cause)**: `driverRef` owns the
  Expo handle; ordinary path never `close()`s it (only Advanced process-
  restart). `AppState` `active` always `recoverAfterLifecycle("foreground")`
  which re-runs `initializeApplication` → `prepareDatabase` `execSync` on
  the existing handle. `lock_resume` is never passed from `App.tsx`.
  Overlapping `runInit` is possible. `ScoringPanel` keeps React history when
  `driver` becomes null.
- **Device**: unlocked; history open; not on the pin pad. Check game
  **8:15:39 AM / 289**. Same pid **31963**.
- **~30s background then HOT foreground**: no banner; 289 still shown.
- **~2 min background then HOT foreground**: **REPRODUCED**. Same
  `NativeDatabase.execSync` / `NullPointerException` banner. Pid unchanged.
  Stopped; no Open after failure; no lock cycle; no force-stop; no
  recovery. Visible 289 / history after fail is **stale React state**, not a
  proven post-failure SQLite read.
- **Result**: `REPRODUCED`. `ROOT_CAUSE=UNCONFIRMED`.
  `COLD_START_RECOVERY=SUCCESS_PREVIOUSLY_OBSERVED`.
  `OVERALL_ACCEPTANCE=HOLD`. `FIELD_VALIDATION=PAUSED`.
  `PIN_DETAIL_AND_ANALYSIS=DEFERRED_REQUIREMENTS`.

### SQLite lifecycle recovery fix (this package; uncommitted)

- **Work package**: `BOWLING_SQLITE_LIFECYCLE_RECOVERY`.
- **Change**: Probe live `exec` (`SELECT 1`) on foreground. On a native-like
  failure, **one** close + same-file `openDatabaseSync` (`bowling-arch001.db`)
  then init. Init serialized so a stale ticket cannot publish a driver.
  Persistent failure stays `DATABASE_OPEN_FAILED` with `[open]|[prepare]|[identity]`.
  Leftover scorecard is labeled not a live read; actions disabled. HOT
  resume does not remount the panel (selection kept). Process restart remounts
  (newest). No DB delete/rename, no new DB name, no pin-detail.
- **Native cause**: still unconfirmed (OEM freeze vs Expo pointer vs other).
  Tests/device PASS do not prove the OEM/native mechanism.
- **Automated**: root `171/171` PASS; server `39/39` PASS on
  `bowling-pg-test-tmp` `127.0.0.1:15433` then stopped, volume preserved;
  both typechecks PASS.
- **APK**: SHA-256
  `78f3b12cc3686e46f1139a422447824f8c12ad216d5586347cee64aadbe4ef09`.
  Signing `fac61745…`. `adb install -r` Success. `firstInstallTime`
  `2026-09-07 12:13:15`. Pulled installed `base.apk` matches.
  `lastUpdateTime=2026-09-13 10:41:30`.
- **Device** pid **23573** HOT: 30s PASS; two ~2 min HOME/HOT PASS; Open
  **8:15:39 AM / 289** after each. Cold restart pid **26567**, newest
  **9:42:04 AM**; listed games include 9:12, 8:15, 8:10, 11:50, 11:34.
- **Result**: `READY_FOR_ACCEPTANCE_REVIEW`. `OVERALL_ACCEPTANCE=HOLD`.
  `FIELD_VALIDATION=PAUSED`. No commit/push/tag/NAS.

### SQLite recovery P2 closure (this package; uncommitted)

- **Work package**: `BOWLING_SQLITE_RECOVERY_P2_CLOSURE`.
- **Scope**: the two recorded lifecycle P2s only. First-use / tenth-frame
  work preserved. No scoring, sync, dependency, or database-file changes.
- **P2 connection cache**: Expo SQLite 57.0.2 `SQLiteOpenOptions.useNewConnection`
  (default false). Recovery opens the same `bowling-arch001.db` with
  `{ useNewConnection: true }`. Launch still uses the default open.
- **P2 coverage**: tests call `runLifecycleRecoveryPass` /
  `createSqliteLifecycleController` (not a reconstruction). Covers post-probe
  `exec` failure, one fresh open, close failure classification (never
  `closed` on throw), explicit reopen/init failure, overlapping events.
- **Close policy**: proceed after `closed` / `already_closed` / `not_needed`,
  or native-like `close_failed`; otherwise fail explicitly and do not open
  a second connection. One recovery attempt per lifecycle event.
- **Diagnostics**: Advanced shows recoveryAttempted / close / newConnection /
  init / published / injected / stages. One-shot post-probe fault is off by
  default, Advanced-armed, auto-consumed, launch does not consume it.
  **Dormant control remains** in Advanced after disarm.
- **Automated**: root `178/178` PASS; server `39/39` PASS on existing
  `bowling-pg-test-tmp` `127.0.0.1:15433` then stopped, volume preserved;
  both typechecks PASS.
- **APK**: SHA-256
  `94881bdf05d55c1e9069aebd1bd0c32db68f3941c26ea8ccfbb026a379289405`.
  `adb install -r` Success. `firstInstallTime=2026-09-07 12:13:15`.
  Pulled `base.apk` matches. `lastUpdateTime=2026-09-13 14:51:52`.
- **Device natural ~2 min HOME/HOT**: pid **12267**, `LaunchState=HOT`,
  Open **8:15:39 AM / 289**, diagnostic
  `recoveryAttempted=false … stages=probe_ok,init_ok,published`.
  **Type B**: resume succeeded **without** exercising native reopen.
  Historical 2-minute NPE and earlier type-B resumes remain on the record.
- **Device injected (labeled)**: Advanced “Arm one recovery test” then
  “Recover from database”. Stages
  `probe_ok,injected_post_probe_fault,recovery_attempted,close:closed,open_fresh,probe_ok,init_ok,published`.
  Proves defensive recovery under the injected post-probe fault, **not**
  the original OEM/native cause. Then disarmed. 8:15 still **289**; user
  games including **9:42:04 AM** still listed.
- **Result**: `READY_FOR_ACCEPTANCE_REVIEW`. `OVERALL_ACCEPTANCE=HOLD`.
  `FIELD_VALIDATION=PAUSED`. No commit/push/tag/NAS.

### SQLite recovery P2 acceptance (this package; documentation only)

- **Work package**: `BOWLING_SQLITE_RECOVERY_P2_ACCEPTANCE`.
- **Provenance**: HEAD `94cfc4b8…` on `codex/first-use-scoring-presentation`.
  Local `app-release.apk` SHA-256 still
  `94881bdf05d55c1e9069aebd1bd0c32db68f3941c26ea8ccfbb026a379289405`.
  Recorded automated gates (not rerun): root `178/178`, server `39/39`,
  both typechecks PASS. No rebuild, install, or device run in this package.
- **P2 connection cache = CLOSED**. Recovery
  `openMobileDatabase({ freshNativeConnection: true })` requests Expo
  `useNewConnection: true` for the same `bowling-arch001.db`. Launch uses
  the default open. Close outcomes are classified; `closed` is never
  reported on throw. `detachAfterClose` always drops the JS current to
  null. Controller `current` is only the pass result; unpublished failures
  null the driver; ScoringPanel receives `getDriver()` only when published
  ready. Non-native `close_failed` stays `DATABASE_OPEN_FAILED` and does
  not open a second connection. Native-like `close_failed` may proceed to
  a requested fresh connection after detach; that does **not** prove Expo
  native ownership is fully exclusive (`useNewConnection` is requested, not
  a complete native-ownership proof). Original OEM NPE remains unconfirmed.
- **P2 recovery coverage = CLOSED**. `tests/mobile.lifecycle-handle.test.ts`
  `"post-probe native-like init failure recovers with one fresh connection"`
  calls `runLifecycleRecoveryPass` (the production orchestration). Device
  **INJECTED** Advanced line recorded
  `probe_ok,injected_post_probe_fault,recovery_attempted,close:closed,open_fresh,probe_ok,init_ok,published`
  then persistence reread **8:15:39 AM / 289**. Distinct from type-B natural
  resume (`recoveryAttempted=false … probe_ok,init_ok,published`, pid 12267).
  Historical 2-minute HOT FAIL (pid 31963) and hung `adb install` (lock /
  Play Protect) remain failed/hung records, not rewritten as PASS.
- **Diagnostic control**: not changed. Off by default, in-memory, one-shot,
  Advanced-labeled, does not mutate facts. Ordinary scoring/history does not
  arm it. After an explicit Arm, the next `AppState` `active` or Recover
  can consume it. **Retain in this local acceptance candidate.** Not
  approved production behavior. Overall acceptance requires a separate
  retain / remove / harden authorization.
- **Result**: `PASS` for these two P2s. `OVERALL_ACCEPTANCE=HOLD`.
  `FIELD_VALIDATION=PAUSED`. `APPLICATION_CODE_CHANGED=false`. No commit.

### Local diagnostic disposition (this package; documentation only)

- **Work package**: `BOWLING_LOCAL_DIAGNOSTIC_DISPOSITION`.
- **Candidate**: HEAD `94cfc4b8…`, APK SHA-256 `94881bdf…` (local file
  hash match). Control unchanged: off by default, in-memory, one-shot,
  Advanced-labeled, armed only by deliberate tap, non-mutating, not reachable
  from ordinary scoring/history. `INITIAL_SHELL_DISCLOSURES.diagnosticsOpen`
  remains false.
- **Decision**: `DIAGNOSTIC_DISPOSITION=RETAIN_LOCAL_ACCEPTANCE_ONLY`.
  `PRODUCTION_DIAGNOSTIC_APPROVAL=false`.
- **Closed findings preserved**: `P2_CONNECTION_CACHE=CLOSED`,
  `P2_RECOVERY_COVERAGE=CLOSED`, `TENTH_FRAME_DEFECT=CLOSED`,
  `INVALID_REPAIR_UI=PASS` (agent-operated, not user comprehension).
- **Current acceptance gates**: user confirmation that first-use is
  accepted after the closed tenth-frame defect, using the already-recorded
  2026-09-12 observations (purpose understood; start simple; entry except
  that defect; correction with a preference for direct frame selection;
  reopen). Not a new walkthrough. Overall first-use still not established
  until that confirmation. `FIELD_VALIDATION=PAUSED` is a later unexecuted
  gate, not a current first-use package blocker.
- **Deferred product requirements (not current blockers)**: direct frame
  selection for correction; pinfall plus numbered standing-pin observations
  and per-game/historical analysis (recorded, not implemented).
- **Original native SQLite cause**: unconfirmed.
- **Result**: `PASS`. `OVERALL_ACCEPTANCE=HOLD`. No application change,
  commit, or device run.

### User first-use acceptance and history new-game placement (this package)

- **Work package**: `BOWLING_HISTORY_NEW_GAME_PLACEMENT`.
- **User decision**: explicit “No Obstacles. Accept.”
  `USER_FIRST_USE_DECISION=ACCEPT`. `USER_REPORTED_OBSTACLES=NONE`.
  `COACHING_HISTORY=NOT_CONFIRMED` (not inferred).
  Acceptance applies to the prior local candidate APK `94881bdf…`.
- **Requested follow-on**: Start a new game at the top of Previous games,
  above the list; no second copy at the bottom of history.
- **Change**: `scoringPanel.tsx` history view; helpers in
  `shellPresentation.ts`; focused test in `tests/shell.presentation.test.ts`.
  `onNewGame` / `startGame` unchanged. Scoring chrome still shows Start a
  new game when history is closed. No scoring/lifecycle/persistence change.
- **Focused tests**: `shell.presentation`, `scoring.history`,
  `mobile.scoring-flow` 30/30 PASS. Both typechecks PASS. Server tests not
  rerun.
- **APK**: SHA-256
  `8edb6e2dda5818c7594cd010117d804d9b80332f7a40d23c34e37f279333e786`.
  `adb install -r` Success. Pulled `base.apk` matches.
  `firstInstallTime=2026-09-07 12:13:15`.
  `lastUpdateTime=2026-09-13 19:16:59`.
- **Device layout (agent, not the user)**: history dump shows one
  **Start a new game** above the list (y 624) before first row (y 836).
  Opening history did not create a game. One new game **7:21:04 PM** from
  that control; **9:42:04 AM**, **8:15:39 AM**, and prior acceptance games
  remain listed. `USER_TESTED_UPDATED_PLACEMENT=false`.
- **Result**: `READY_FOR_LOCAL_BASELINE_REVIEW`. Production unauthorized.
  `FIELD_VALIDATION=PAUSED`. No commit.

### ADR-007 requirements acceptance (this package; documentation only)

- **Work package**: `BOWLING_ADR_007_REQUIREMENTS_ACCEPTANCE`.
- **Baseline verified**: `624f1ccd…` / tree `38a99b05…` on
  `arch/001-domain-sync-foundation`; origin `Drago1068/Bowling`. Slice 6
  tag remains `v0.6.0-arch001-slice6` → `94cfc4b8…` (distinct). Expected
  dirty set only: `PROJECT_STATUS.md` + `docs/adr/ADR-007-…`.
- **Document**: `docs/adr/ADR-007-pin-detail-and-analysis-requirements.md`
  (**ACCEPTED**; implementation unauthorized).
- **Accepted**: U1–U3; incomplete/repair exclude from rates + list with
  counts; common leaves = frame-opening 1–10, nonempty recorded only;
  first-ball metrics denominators explicit; spares 1–9; unknown vs empty
  identity rules; correction integrity; B1 = capture + direct Fix only;
  `PinLeave` writes false; analysis not in B1; trends deferred.
- **Outstanding for B1 auth**: resolved into proposed technical contract
  ADR-008 (awaiting AA approval); generic `canonical_entities` already
  stores any entity type — gap is write path, multi-entity TX, and
  proposed server PinState validation.
- **Result**: `PASS`. `IMPLEMENTATION_AUTHORIZED=false`.
  `FIELD_VALIDATION=PAUSED`. No application code, tests, migrations,
  commits, or tags.

### ADR-008 technical acceptance (this package; documentation only)

- **Work package**: `BOWLING_ADR_008_TECHNICAL_ACCEPTANCE`.
- **Baseline verified**: `624f1ccd…` / tree `38a99b05…`; dirty docs only.
- **Document**: `docs/adr/ADR-008-b1-pinstate-persistence-sync-contract.md`
  (**ACCEPTED**; implementation unauthorized).
- **Safeguards recorded**: fail-closed pre-index duplicate scan; identical
  active/roll_id semantics; legacy without basis never EFFECTIVE; basis
  necessary not sufficient (+ rack rules); PG savepoint then conflict with
  incoming payload; CREATE-before-DELETE = terminal CONFLICT + fresh
  submission after archive; pull association collision keeps local, stores
  remote in conflict record, advances checkpoint.
- **Result**: `PASS`. `ADR_008_ACCEPTED=true`.
  `B1_TECHNICAL_CONTRACT=ACCEPTED`. `IMPLEMENTATION_AUTHORIZED=false`.
  `UNRESOLVED_DECISIONS=none`.

### B1 pin detail capture implementation (prior package)

- **Work package**: `BOWLING_B1_PIN_DETAIL_CAPTURE_IMPLEMENTATION`.
- **Branch**: `codex/b1-pin-detail-capture` from baseline `624f1ccd…`.
- **Evidence**: `docs/evidence/arch-001-b1-pin-detail-capture.md`.
- **Automated**: root **188/188**, server **41/41** on
  `bowling-pg-test-tmp` `:15433` (stopped, volume preserved), both typechecks
  PASS.
- **Device (then)**: HOLD under mistaken android-absent reading.

### B1 Android build recovery and device acceptance (prior package)

- **Work package**: `BOWLING_B1_BUILD_RECOVERY_DEVICE_ACCEPTANCE`.
- **Android tree**: present, Expo-generated, gitignored (`/android`); not missing
  (earlier HOLD was a tooling false negative).
- **APK SHA-256 (then)**: `2a676cf6847bb34bbc29a8eae256fb37cb9f507a76ee3b604b6042f29cd21109`.
- **Device**: **PASS** with noted Fix Cancel below-fold workaround (cold restart once).

### B1 Fix-mode exit accessibility (this package)

- **Work package**: `BOWLING_B1_FIX_EXIT_ACCESSIBILITY`.
- **Change**: presentation-only Fix layout — Cancel above capped chooser; while
  editing, chooser/scorecard hidden, Cancel with editor, explicit Save standing.
- **APK SHA-256**: `43aaf3b1b59301cdfd43778e845a12a716435f6bbd364ce5e704df2b22104526`.
- **Tests**: FIX_EXIT presentation tests PASS; both typechecks PASS.
- **Device**: long-game Save/Cancel without force-stop **PASS**;
  `RESTART_WORKAROUND_REQUIRED=false`. Existing `9/13/2026` games preserved.
- **Result**: `READY_FOR_USER_WALKTHROUGH`. `B1_FORMAL_ACCEPTANCE=false`.
  `USER_PIN_SELECTOR_ACCEPTANCE` later set to **HOLD** (confusing entry).
  No commits/pushes/tags. B2 not started.

### Graphical entry redesign prototype

- **Work package**: `BOWLING_GRAPHICAL_ENTRY_PROTOTYPE` (initial).
- **User**: rejected current B1 entry as confusing →
  `USER_PIN_SELECTOR_ACCEPTANCE=HOLD` /
  `REASON=USER_REPORTS_CONFUSING_ENTRY`.
- **Proposal (pending)**: rack-first; selected = standing; scorecard X/; frame-tap
  correction; explicit Save; unknown prior rack → no invented identities.
- **Does not** rewrite accepted ADR-007 U2 as approved.

### Graphical entry prototype refinement (this package)

- **Work package**: `BOWLING_GRAPHICAL_ENTRY_PROTOTYPE_REFINEMENT`.
- **Feedback**: selected=standing YES; rack-first YES with changes; frame-tap YES.
  `USER_DESIGN_ACCEPTANCE=CHANGES_REQUESTED` then refined → re-review **PENDING**.
- **Refinements in prototype**:
  1. Tenth-frame strikes display **X** (`X X X`, `X 9 /`, `9 / X`).
  2. Context-aware **Spare** after non-strike first ball (incl. miss) and tenth `X 9`.
  3. **Save** auto-advances; remaining rack carried forward; explicit Save preserved.
- **Prototype**: `prototypes/graphical-entry/index.html` (isolated; fictional only).
- **Production app / APK**: unchanged
  (`43aaf3b1b59301cdfd43778e845a12a716435f6bbd364ce5e704df2b22104526`).
- **Result**: `READY_FOR_USER_REVIEW`. Capture-default approval still pending.

#### Exact next prompt (do not execute)

```text
# BOWLING — GRAPHICAL ENTRY REFINEMENT FEEDBACK
Open prototypes/graphical-entry/index.html.
Check only whether these three refinements behave as expected:
1) Tenth strikes show X (e.g. X X X, X 9 /, 9 / X) — not numeric 10.
2) Spare is offered after a non-strike first ball (including miss) and for tenth X 9.
3) Save advances to the next legal delivery with the remaining rack carried forward.
Accept, request more changes, or reject. Do not implement production changes.
STOP.
```